# Database
DATABASE_URL=postgresql://username:password@host:port/database

# JWT Secret for authentication
JWT_SECRET=your-super-secret-jwt-key-here

# Server (Railway will set this automatically)
PORT=3000
NODE_ENV=production
